# 本仓库根目录 .gitignore（与内核自带的各子目录 .gitignore 并存）

# 构建输出（产物在 out/arch/arm64/boot/Image 等）
/out/

# 实机提取时产生的较大二进制（保留 extracted_*/ 下的 config/dtb 即可复现）
/device_extract/imgread/
/device_extract/ko_modules/*.ko
/device_extract/dtc/builddir/

# 可选：若将 aospclang 放在仓库外则无需忽略
# /aospclang/
